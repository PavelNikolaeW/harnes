"""Meta-cycle — внешний синхронный цикл агента.

Этапы: sense → attend → goal_arbitration → recall → react_loop
       → verify → world_update → [reflect] → store.

См. `agent_architecture.html` § 3.

v0: tick driver и stubs этапов — задача #9.
"""
